import { CheckingdataPipe } from '../checkingdata.pipe';

describe('CheckingdataPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckingdataPipe();
    expect(pipe).toBeTruthy();
  });
});
